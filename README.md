# Album Site

Bootstrap 5.3 Album example site.

## Workflow

- Branch names include Jira key (e.g., feature/ALBUM-1-setup-repo)
- Pull Request required to merge into main
- CI checks required to pass before merge
